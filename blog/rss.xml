<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/">
    <channel>
        <title>Kahuna Blog</title>
        <link>https://kahunakv.github.io/blog/</link>
        <description>Kahuna Blog</description>
        <lastBuildDate>Mon, 21 Sep 2026 00:00:00 GMT</lastBuildDate>
        <docs>https://validator.w3.org/feed/docs/rss2.html</docs>
        <generator>https://github.com/jpmonette/feed</generator>
        <language>en</language>
        <item>
            <title><![CDATA[290k Requests per Second on One Node, and Faster Than Valkey]]></title>
            <link>https://kahunakv.github.io/blog/290k-requests-per-second/</link>
            <guid>https://kahunakv.github.io/blog/290k-requests-per-second/</guid>
            <pubDate>Mon, 21 Sep 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[This week Kahuna crossed a line I did not expect to cross this year. A single node served 290k get requests per second for ephemeral keys. On the same machine, with the same number of clients, Valkey 9.1 served 247k plain GET requests per second.]]></description>
            <content:encoded><![CDATA[<p>This week Kahuna crossed a line I did not expect to cross this year. A single node served 290k <code>get</code> requests per second for ephemeral keys. On the same machine, with the same number of clients, Valkey 9.1 served 247k plain <code>GET</code> requests per second.</p>
<p>The rate limiter is the number I care about more. Kahuna ran the fixed-window counter script from the <a class="" href="https://kahunakv.github.io/docs/recipes/rate-limiting/">rate-limiting recipe</a> at about 200k requests per second. Valkey ran the same counter as a Lua script at 161k with <code>EVAL</code> and 175k with <code>EVALSHA</code>.</p>
<p>Before this work the same rate limiter ran at 84k. Nothing about the storage engine changed. What changed is how requests travel over the wire, and how a script that touches one key runs inside the node.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-numbers-and-what-they-do-not-mean">The Numbers, and What They Do Not Mean<a href="https://kahunakv.github.io/blog/290k-requests-per-second/#the-numbers-and-what-they-do-not-mean" class="hash-link" aria-label="Direct link to The Numbers, and What They Do Not Mean" title="Direct link to The Numbers, and What They Do Not Mean" translate="no">​</a></h2>
<p>All numbers come from one 8-core Apple Silicon Mac. The Kahuna node and the benchmark client ran on the same machine and competed for the same cores. Valkey 9.1.2 and <code>valkey-benchmark</code> ran on the same machine the same way. Every run used 64 closed-loop workers, a key space of 10,000 keys, and 128-byte values. The Valkey runs used no pipelining. The machine was CPU-saturated in every run.</p>
<table><thead><tr><th>Workload</th><th style="text-align:right">Kahuna, before</th><th style="text-align:right">Kahuna, now</th><th style="text-align:right">Valkey, same machine</th></tr></thead><tbody><tr><td>Ephemeral <code>get</code></td><td style="text-align:right">104k req/s</td><td style="text-align:right">290k req/s</td><td style="text-align:right">247k req/s (<code>GET</code>)</td></tr><tr><td>Rate-limit counter script</td><td style="text-align:right">84k req/s</td><td style="text-align:right">194k-203k req/s</td><td style="text-align:right">175k req/s (Lua <code>EVALSHA</code>)</td></tr><tr><td>Ephemeral <code>set</code></td><td style="text-align:right">84k req/s</td><td style="text-align:right">218k req/s</td><td style="text-align:right"></td></tr><tr><td>Mixed <code>get</code>/<code>set</code></td><td style="text-align:right">104k req/s</td><td style="text-align:right">234k req/s</td><td style="text-align:right"></td></tr><tr><td>Script <code>RETURN 1</code></td><td style="text-align:right">105k req/s</td><td style="text-align:right">416k-438k req/s</td><td style="text-align:right"></td></tr></tbody></table>
<p>Latency moved the same way. The rate limiter went from p50 700 µs and p99 4.0 ms to p50 284 µs and p99 0.7 ms at the same concurrency.</p>
<p>Read these as a comparison, not as a capacity claim. Run-to-run noise on this machine is about ±15%. A client on a second machine would give higher numbers for the node alone. A three-node cluster with persistent, replicated writes is a different benchmark, and the <a class="" href="https://kahunakv.github.io/docs/benchmarking/#recent-local-results">benchmarking page</a> keeps those separate on purpose.</p>
<p>The Kahuna side is also a memory node with an ephemeral durability level. That is the fair comparison with Valkey. It is the same class of state: short-lived, in memory, rebuildable. It is not the state you put behind Raft.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="where-the-time-went">Where the Time Went<a href="https://kahunakv.github.io/blog/290k-requests-per-second/#where-the-time-went" class="hash-link" aria-label="Direct link to Where the Time Went" title="Direct link to Where the Time Went" translate="no">​</a></h2>
<p>Before this work, a script that did nothing at all cost the node about 40 µs of CPU per request. <code>RETURN 1</code> touches no key. It runs no transaction. It still topped out near 105k requests per second, and the node was busy the whole time.</p>
<p>That cost was not in Kahuna's storage or its actors. It was in the transport. The .NET client sends every key/value request over one long-lived bidirectional gRPC stream per connection. Each request was its own stream message, and each response was its own stream message.</p>
<p>A stream message has a fixed price that does not depend on what the request does: a gRPC frame, an HTTP/2 DATA frame, the transport's pipe locks, the protobuf envelope, and five or six thread hops on each side. For a small request that fixed price is most of the total.</p>
<p>The rate limiter had a second problem, inside the node. An admitted request is a pessimistic single-key transaction. On the general path it sends six messages to the actor that owns the key: lock, read, write, prepare, a range-lock probe, and commit. Each message is routed, queued on the actor's mailbox, and awaited. All six go to the same single-threaded actor.</p>
<p>Two changes removed most of both costs.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="change-one-request-frames">Change One: Request Frames<a href="https://kahunakv.github.io/blog/290k-requests-per-second/#change-one-request-frames" class="hash-link" aria-label="Direct link to Change One: Request Frames" title="Direct link to Change One: Request Frames" translate="no">​</a></h2>
<p>A <strong>frame</strong> is one gRPC stream message that carries several independent requests, or several independent responses. The fixed cost of the message is paid once per frame instead of once per request.</p>
<p>The rule that makes frames safe is that a frame never waits. The client packs only the requests that are already waiting when it writes. The node packs only the responses that are already ready. No request is held back to make a frame fuller. At low load a request travels alone, as the plain message it always was, and a quiet connection is byte-identical to a connection without frames.</p>
<p>Each item in a frame is a complete request with its own type and its own id. The node runs every item through the same code path as a lone request. An item that fails does not affect the others. A frame does not make its items atomic, and it does not order them. It is a transport optimization only. Atomicity still comes from a script or a transaction.</p>
<p>Frames are negotiated per stream, and never by probing. A node that reads frames says so in a response header when the stream opens. The client sends a frame only after it sees that header on that stream. The node sends response frames only on a stream that already carried a request frame. A new client works with an old node, an old client works with a new node, and a rolling upgrade is safe in either order.</p>
<p>A frame holds at most 256 items or 1 MiB of serialized items. The byte budget sits far below the 4 MB gRPC message limit on purpose. A message over that limit resets the stream that every other request shares.</p>
<p>The effect on the empty script shows what the transport was costing:</p>
<table><thead><tr><th><code>RETURN 1</code></th><th style="text-align:right">One request per message</th><th style="text-align:right">Frames</th></tr></thead><tbody><tr><td>Throughput</td><td style="text-align:right">105k req/s</td><td style="text-align:right">416k-438k req/s</td></tr><tr><td>Node CPU per request</td><td style="text-align:right">39.9 µs</td><td style="text-align:right">9.2 µs</td></tr><tr><td>Client CPU per request</td><td style="text-align:right">25 µs</td><td style="text-align:right">6.6 µs</td></tr><tr><td>p50 / p99 at c=64</td><td style="text-align:right">601 µs / 805 µs</td><td style="text-align:right">140 µs / 317 µs</td></tr></tbody></table>
<p>For <code>get</code> the same change took the node from 104k to 290k requests per second. That is the headline number, and it is a transport win. The storage path for an ephemeral <code>get</code> did not change.</p>
<p>If you have used Redis pipelining, frames are the same idea. The difference is that the client does it for you, on every connection, and only when there is something waiting.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="change-two-one-actor-turn-per-script">Change Two: One Actor Turn per Script<a href="https://kahunakv.github.io/blog/290k-requests-per-second/#change-two-one-actor-turn-per-script" class="hash-link" aria-label="Direct link to Change Two: One Actor Turn per Script" title="Direct link to Change Two: One Actor Turn per Script" translate="no">​</a></h2>
<p>Frames took the rate limiter from 84k to about 138k requests per second. Then the node sat at about 610% CPU while the client used about 110%. The transport was no longer the limit. The six actor messages were.</p>
<p>The insight is that all six messages go to the same single-threaded actor. Most of what the protocol between them protects against, another transaction interleaving between two steps, cannot happen inside one actor turn.</p>
<p>The fix has two parts, and both apply only to the ephemeral key space on the node that leads the key's partition.</p>
<p><strong>Fused finalize.</strong> A transaction whose whole write set is one ephemeral key finalizes with one actor message instead of three. That message runs the existing prepare handler, the existing range-lock check, and the existing commit handler, in that order, in a single actor turn. Six messages become four. This applies to interactive transactions as well as scripts.</p>
<p><strong>Script actor turns.</strong> An auto-commit script whose lock analysis names exactly one ephemeral key runs start to finish inside one turn of the actor that owns the key. The turn issues the same requests to the same handlers in the same order as the general path. Each request is a direct call served by the actor that is already running, not a routed mailbox round trip. Six messages become one.</p>
<p>A script takes a turn only when every statement is one a turn can run: expressions, <code>LET</code>, <code>IF</code>, <code>RETURN</code>, <code>THROW</code>, and the ephemeral point operations. A script that sleeps, loops, scans a prefix, touches a persistent key, or opens an explicit <code>BEGIN</code> stays on the general path. The shape check is a filter, not a proof. If a turn finds a statement it cannot run, it releases the key, discards what it staged, and the script runs again from the start on the general path. Nothing from the first attempt is observable.</p>
<p>Nothing about the answer changes. The result, the revision, the reason text, and the state left on the key are the same on either path. The turn takes the same exclusive lock. Foreign locks and write intents are honoured by the same handler checks. The test suites run every scenario with the mechanisms on and off and compare. The <a class="" href="https://kahunakv.github.io/docs/scripts/single-key-fast-path/">single-key fast path</a> page lists the exact rules and the switches to turn each part off.</p>
<table><thead><tr><th>Rate limiter, frames on</th><th style="text-align:right">req/s</th><th style="text-align:right">p50</th><th style="text-align:right">p99</th></tr></thead><tbody><tr><td>Neither mechanism</td><td style="text-align:right">127k-136k</td><td style="text-align:right">410-420 µs</td><td style="text-align:right">2.4-3.8 ms</td></tr><tr><td>Fused finalize only</td><td style="text-align:right">140k-160k</td><td style="text-align:right">357-384 µs</td><td style="text-align:right">0.8-1.4 ms</td></tr><tr><td>Script actor turns (default)</td><td style="text-align:right">194k-203k</td><td style="text-align:right">284-289 µs</td><td style="text-align:right">0.6-0.8 ms</td></tr></tbody></table>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-this-changes-what-kahuna-is-for">Why This Changes What Kahuna Is For<a href="https://kahunakv.github.io/blog/290k-requests-per-second/#why-this-changes-what-kahuna-is-for" class="hash-link" aria-label="Direct link to Why This Changes What Kahuna Is For" title="Direct link to Why This Changes What Kahuna Is For" translate="no">​</a></h2>
<p>Until now I described Kahuna as the layer between Redis and Postgres. It held the state that was too important for a cache and too hot for a database. I said in <a class="" href="https://kahunakv.github.io/blog/redis-postgres-and-kahuna/">an earlier post</a> that consensus is not free, and that you should pay for it only where a wrong answer is worse than a slow answer.</p>
<p>That advice still holds for persistent, replicated keys. For ephemeral keys it no longer describes the cost. A single Kahuna node now serves counters, flags, and point reads at the speed people choose Redis or Valkey for. That puts a set of workloads on the table that I used to send elsewhere:</p>
<ul>
<li class=""><strong>Rate limiting.</strong> One script, one ephemeral key, atomic check and increment, about 200k decisions per second on one node. The <a class="" href="https://kahunakv.github.io/docs/recipes/rate-limiting/">recipe</a> is written to take the fast path.</li>
<li class=""><strong>Counters, quotas, and feature flags.</strong> The same shape as the rate limiter: read, compare, write, one key.</li>
<li class=""><strong>Idempotency keys and deduplication.</strong> <code>EEXISTS</code> and <code>ESET</code> with an expiry, in one atomic script.</li>
<li class=""><strong>Session and presence state.</strong> Point <code>get</code> and <code>set</code> on keys with a TTL.</li>
<li class=""><strong>Server-side logic.</strong> Kahuna Script runs where the data is, like Lua in Redis, but with the same transaction semantics as the rest of the system.</li>
</ul>
<p>The difference from running these on Redis or Valkey is that the same node, with the same client and the same verbs, also holds the state you cannot lose. A lock, a leader lease, a sequence, or a multi-key transaction lives in the persistent key space, replicated with Raft and tested with Jepsen. You do not run one system for the fast state and another for the correct state. You choose the durability level per key.</p>
<p>I want to be careful about the claim. Valkey is a mature project with years of tuning, and it executes commands on one main thread by design, while Kahuna used every core on the machine. The comparison is fair on the workload and the machine, not on the engineering budget. And these are single-node memory numbers. Replicated, persistent writes cost what they cost.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-is-still-slow">What Is Still Slow<a href="https://kahunakv.github.io/blog/290k-requests-per-second/#what-is-still-slow" class="hash-link" aria-label="Direct link to What Is Still Slow" title="Direct link to What Is Still Slow" translate="no">​</a></h2>
<p>Frames cover the key/value stream: get, set, delete, exists, scripts, bucket reads, prefix scans, and interactive transaction calls. The lock stream and the sequence calls still carry one request per message. Locks ran at 49k requests per second before and after, and sequences at 114k. Both still pay the full per-message cost.</p>
<p>The fast path is deliberately narrow. Multi-key scripts, scripts that scan, and anything on the persistent key space run through the general path with all six messages. An interactive transaction over two ephemeral keys still does two-phase commit and ran at 19k requests per second with frames.</p>
<p>One more thing frames do not fix. A node under sustained fixed-window rate-limit load slows over time, because fixed windows create one new short-lived key per subject per window. The cost of those keys is not a transport cost, and frames do not change it.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="reproduce-it">Reproduce It<a href="https://kahunakv.github.io/blog/290k-requests-per-second/#reproduce-it" class="hash-link" aria-label="Direct link to Reproduce It" title="Direct link to Reproduce It" translate="no">​</a></h2>
<p>Both changes are on by default in the current build. The benchmark tool has switches to turn each one off, so one build runs both arms of the comparison:</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token comment" style="color:#999988;font-style:italic"># Frames on (default)</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">kahuna-bench </span><span class="token parameter variable" style="color:#36acaa">-c</span><span class="token plain"> http://localhost:8083 </span><span class="token parameter variable" style="color:#36acaa">--workload</span><span class="token plain"> rate-limit </span><span class="token parameter variable" style="color:#36acaa">--durability</span><span class="token plain"> ephemeral </span><span class="token punctuation" style="color:#393A34">\</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  --key-space </span><span class="token number" style="color:#36acaa">10000</span><span class="token plain"> --rate-limit-budget </span><span class="token number" style="color:#36acaa">1000000</span><span class="token plain"> </span><span class="token parameter variable" style="color:#36acaa">--duration</span><span class="token plain"> </span><span class="token number" style="color:#36acaa">10</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token comment" style="color:#999988;font-style:italic"># Frames off</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">kahuna-bench </span><span class="token parameter variable" style="color:#36acaa">-c</span><span class="token plain"> http://localhost:8083 </span><span class="token parameter variable" style="color:#36acaa">--workload</span><span class="token plain"> rate-limit </span><span class="token parameter variable" style="color:#36acaa">--durability</span><span class="token plain"> ephemeral </span><span class="token punctuation" style="color:#393A34">\</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  --key-space </span><span class="token number" style="color:#36acaa">10000</span><span class="token plain"> --rate-limit-budget </span><span class="token number" style="color:#36acaa">1000000</span><span class="token plain"> </span><span class="token parameter variable" style="color:#36acaa">--duration</span><span class="token plain"> </span><span class="token number" style="color:#36acaa">10</span><span class="token plain"> --no-request-frames</span><br></div></code></pre></div></div>
<p>Start the node with <code>--disable-script-actor-turns</code> or <code>--disable-fused-ephemeral-finalize</code> to measure the fast path on its own. Restart the node between arms, alternate them, and run the client on a second machine if you want numbers that describe the node rather than the pair. The <a class="" href="https://kahunakv.github.io/docs/benchmarking/">benchmarking page</a> has the full node command line.</p>
<p>Kahuna is <a href="https://github.com/kahunakv/kahuna" target="_blank" rel="noopener noreferrer" class="">open source</a> under the MIT license. If your rate limiter, counter, or session store runs on Redis today, I would like to know how the same script does on Kahuna on your hardware.</p>]]></content:encoded>
            <category>kahuna</category>
            <category>performance</category>
            <category>redis</category>
            <category>valkey</category>
            <category>rate-limiting</category>
            <category>benchmarks</category>
        </item>
        <item>
            <title><![CDATA[Couldn't You Just Use Redis and Postgres?]]></title>
            <link>https://kahunakv.github.io/blog/redis-postgres-and-kahuna/</link>
            <guid>https://kahunakv.github.io/blog/redis-postgres-and-kahuna/</guid>
            <pubDate>Thu, 10 Sep 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[Last Sunday, I was having lunch with a friend and talking about Kahuna, CamusDB, and all the time I've been putting into these projects lately. He didn't really understand why I would build Kahuna when most of what it does could probably be achieved with some combination of Redis and Postgres.]]></description>
            <content:encoded><![CDATA[<p>Last Sunday, I was having lunch with a friend and talking about Kahuna, <a href="https://camusdb.github.io/" target="_blank" rel="noopener noreferrer" class="">CamusDB</a>, and all the time I've been putting into these projects lately. He didn't really understand why I would build Kahuna when most of what it does could probably be achieved with some combination of Redis and Postgres.</p>
<p>My explanation may not have been very convincing, and I understand why. Every business has a different tolerance for durability, availability, and consistency. Not every business operates 24/7, and not every business suffers significantly if its systems are down for a few minutes or even a few hours. In many operations, problems and mistakes can be fixed manually by people, or sometimes with an apology to the customer.</p>
<p>My story is very different. For some reason, I've spent almost my entire career working in industries that operate 24/7: airline reservations, hospitality, and video games. There is always a customer or user somewhere interacting with the system, whether it's just a few people or hundreds of thousands at the same time. That changed the way my brain works and the kinds of failure modes I instinctively try to avoid.</p>
<p>Building systems where performance is high and consistency is non-negotiable gradually became part of my DNA.</p>
<p>This post is the answer I wish I had given at lunch. It is not an attack on Redis or Postgres. I use both, I like both, and I will keep recommending them. It is about the space between them, and why I built Kahuna to fill it.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-space-between-them">The Space Between Them<a href="https://kahunakv.github.io/blog/redis-postgres-and-kahuna/#the-space-between-them" class="hash-link" aria-label="Direct link to The Space Between Them" title="Direct link to The Space Between Them" translate="no">​</a></h2>
<p>Redis is fast, simple, and a pleasure to use. Postgres is the database I trust with real money. But in every 24/7 system I worked on, there was a type of state that was too important for the cache and too hot for the database.</p>
<p>Which worker owns this job? Which node is the leader? What is the next reservation number? Is this seat still free? Did we already process this webhook?</p>
<p>This state is small, it changes all the time, and every service reads it. A wrong answer is not a small inconvenience. It is a double charge, a double booking, or a leader that does not know it stopped being the leader.</p>
<p>The usual solution is to keep the hot part in Redis and the durable part in Postgres, and write application code to keep both copies in agreement. Then you handle the case where the database write succeeds and the cache update fails, the opposite case, and the case where the two disagree. Every team writes its own version of that code, and every version has its own bugs.</p>
<p>Kahuna exists because I wanted one place for that type of state, with the guarantees built in instead of rebuilt in every service.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="distributed-locks">Distributed Locks<a href="https://kahunakv.github.io/blog/redis-postgres-and-kahuna/#distributed-locks" class="hash-link" aria-label="Direct link to Distributed Locks" title="Direct link to Distributed Locks" translate="no">​</a></h2>
<p>The classic Redis lock is <code>SET key value NX PX 30000</code>. It works very well, until it doesn't. Redis replication is asynchronous, so a failover can forget a lock that was confirmed a moment ago, and a second client happily takes it. Redlock tries to fix that with several servers, but it still has the same missing piece: when a lock expires while its owner is paused, you have two owners, and nothing tells the protected resource which one is old.</p>
<p>Postgres advisory locks live inside a real transactional system, but they belong to a session. If the connection drops or the primary fails over, the lock is gone. If the holder pauses for a long garbage collection, it still believes it owns the lock.</p>
<p>Kahuna locks are designed around the failure cases:</p>
<ul>
<li class="">Lock state is replicated through Raft, so a leader change does not forget who owns what.</li>
<li class="">Every lock is a lease with an expiration, so a crashed owner cannot hold a resource forever.</li>
<li class="">Every acquisition returns a <strong>fencing token</strong> that only grows. A database row, a payment API, or a file can reject any operation with an older token. Two owners after a pause is still possible in any lease-based system. Two owners <em>causing damage</em> is not.</li>
</ul>
<p>A lock without a fencing token is a suggestion. A lock with one is a guarantee you can enforce. The <a class="" href="https://kahunakv.github.io/blog/jepsen-testing-kahuna/">Jepsen suite</a> checks every night that fencing tokens never move backwards, even during partitions, process kills, and leader elections.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="keyvalue-store">Key/Value Store<a href="https://kahunakv.github.io/blog/redis-postgres-and-kahuna/#keyvalue-store" class="hash-link" aria-label="Direct link to Key/Value Store" title="Direct link to Key/Value Store" translate="no">​</a></h2>
<p>"That is just Redis, or a table with two columns." It is, until you need several things at the same time.</p>
<p>You need durability, so a failover does not lose a confirmed value. Redis gives you speed and asynchronous replication. Postgres gives you durability, but every hot read now goes through a connection pool and a buffer cache to fetch a 40-byte value.</p>
<p>You need volume. Sending thousands of small key/value operations per second to Postgres is not a good idea. Each one takes a connection, a parse, a plan, and a lock, and all of that competes with the real business queries. The whole reason a cache exists is to take that load <em>away</em> from the database.</p>
<p>You need conditional and multi-key writes, because the seat map, the hold, and the reservation counter must change together or not at all. Redis has <code>WATCH</code> and Lua on one node. Postgres has real transactions, but then the reads are back on the slow path.</p>
<p>Kahuna brings those properties together:</p>
<ul>
<li class="">Persistent keys are replicated through Raft and stored on disk. Ephemeral keys skip the disk. Same API, and you decide per key.</li>
<li class="">Hot state is served from memory on the partition leader, so reads do not pay a database round trip.</li>
<li class="">Every key has a revision, so compare-and-set is the default tool, not a script you maintain.</li>
<li class="">Transactions cover several keys and partitions, with snapshot isolation or serializable consistency. The <a class="" href="https://kahunakv.github.io/blog/reusable-transaction-coordinator/">transaction coordinator</a> tracks the working set on the server, so a retried request cannot lose a write or apply it twice.</li>
</ul>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="sequencer">Sequencer<a href="https://kahunakv.github.io/blog/redis-postgres-and-kahuna/#sequencer" class="hash-link" aria-label="Direct link to Sequencer" title="Direct link to Sequencer" translate="no">​</a></h2>
<p>Postgres has <code>SEQUENCE</code>. Redis has <code>INCR</code>. Both are correct on a healthy network.</p>
<p>Now imagine a lost reply. The server allocated 501, and the client never received it. If the client retries, it receives 502, and 501 is wasted or, worse, already used. If it does not retry, it has no number. Neither system can say "you already asked for this one, here it is again", because neither one remembers the request, only the counter.</p>
<p>Kahuna sequences accept an <strong>idempotency key</strong>. Retry the same request and you receive the same allocation. Sequences are replicated through Raft, so a leader change does not rewind them, and workers can reserve a range of numbers in one call. Invoice numbers, ticket numbers, and order IDs are the places where a duplicate found a week later is not an acceptable answer.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="scaling-without-a-maintenance-window">Scaling Without a Maintenance Window<a href="https://kahunakv.github.io/blog/redis-postgres-and-kahuna/#scaling-without-a-maintenance-window" class="hash-link" aria-label="Direct link to Scaling Without a Maintenance Window" title="Direct link to Scaling Without a Maintenance Window" translate="no">​</a></h2>
<p>Redis scales in a fairly static way. Redis Cluster spreads keys over fixed hash slots, but adding a node moves no data by itself. Somebody has to run a resharding command and watch it. Nothing rebalances on its own. Postgres can add read replicas, but writes still go to one primary.</p>
<p>Kahuna can change shape while it keeps serving traffic:</p>
<ul>
<li class=""><strong>Add nodes</strong> to a running cluster. A new node joins as a learner, catches up, and takes its share of partitions.</li>
<li class=""><strong>Remove nodes.</strong> A graceful leave copies the departing node's replicas to the survivors before it leaves.</li>
<li class=""><strong>Self-balance.</strong> The placement rebalancer and the leader balancer move replicas and leadership away from busy nodes, in small steps, in the background.</li>
<li class=""><strong>Add partitions for hot data.</strong> With key-range routing, a range that grows too large or receives too many writes is split automatically.</li>
</ul>
<p>Scaling stops being an event you plan for a Sunday night and becomes a change the cluster absorbs while customers keep booking seats.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="it-sounds-complicated-the-api-is-not">It Sounds Complicated. The API Is Not.<a href="https://kahunakv.github.io/blog/redis-postgres-and-kahuna/#it-sounds-complicated-the-api-is-not" class="hash-link" aria-label="Direct link to It Sounds Complicated. The API Is Not." title="Direct link to It Sounds Complicated. The API Is Not." translate="no">​</a></h2>
<p>All of this lives on the server. The surface you use every day is the same small set of verbs you already know from Redis: <code>set</code>, <code>get</code>, <code>delete</code>, <code>lock</code>, <code>unlock</code>, <code>extend</code>.</p>
<div class="language-kahuna codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-kahuna codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token variable" style="color:#36acaa">kahuna</span><span class="token operator" style="color:#393A34">-</span><span class="token variable" style="color:#36acaa">cli</span><span class="token operator" style="color:#393A34">&gt;</span><span class="token plain"> </span><span class="token command builtin">set</span><span class="token plain"> </span><span class="token variable" style="color:#36acaa">flags</span><span class="token operator" style="color:#393A34">/</span><span class="token variable" style="color:#36acaa">checkout</span><span class="token operator" style="color:#393A34">-</span><span class="token variable" style="color:#36acaa">v2</span><span class="token plain"> </span><span class="token string" style="color:#e3116c">"enabled"</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token variable" style="color:#36acaa">r0</span><span class="token plain"> </span><span class="token command builtin">set</span><span class="token plain"> 9ms</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token variable" style="color:#36acaa">kahuna</span><span class="token operator" style="color:#393A34">-</span><span class="token variable" style="color:#36acaa">cli</span><span class="token operator" style="color:#393A34">&gt;</span><span class="token plain"> </span><span class="token command builtin">get</span><span class="token plain"> </span><span class="token variable" style="color:#36acaa">flags</span><span class="token operator" style="color:#393A34">/</span><span class="token variable" style="color:#36acaa">checkout</span><span class="token operator" style="color:#393A34">-</span><span class="token variable" style="color:#36acaa">v2</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token variable" style="color:#36acaa">r0</span><span class="token plain"> </span><span class="token variable" style="color:#36acaa">enabled</span><span class="token plain"> 7ms</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token variable" style="color:#36acaa">kahuna</span><span class="token operator" style="color:#393A34">-</span><span class="token variable" style="color:#36acaa">cli</span><span class="token operator" style="color:#393A34">&gt;</span><span class="token plain"> </span><span class="token variable" style="color:#36acaa">lock</span><span class="token plain"> </span><span class="token variable" style="color:#36acaa">jobs</span><span class="token operator" style="color:#393A34">/</span><span class="token variable" style="color:#36acaa">nightly</span><span class="token operator" style="color:#393A34">-</span><span class="token variable" style="color:#36acaa">invoices</span><span class="token plain"> </span><span class="token number" style="color:#36acaa">30000</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token variable" style="color:#36acaa">f1</span><span class="token plain"> </span><span class="token variable" style="color:#36acaa">acquired</span><span class="token plain"> 786f947d8a6643f0b939865f72aa512a</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token variable" style="color:#36acaa">kahuna</span><span class="token operator" style="color:#393A34">-</span><span class="token variable" style="color:#36acaa">cli</span><span class="token operator" style="color:#393A34">&gt;</span><span class="token plain"> </span><span class="token variable" style="color:#36acaa">unlock</span><span class="token plain"> </span><span class="token variable" style="color:#36acaa">jobs</span><span class="token operator" style="color:#393A34">/</span><span class="token variable" style="color:#36acaa">nightly</span><span class="token operator" style="color:#393A34">-</span><span class="token variable" style="color:#36acaa">invoices</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token variable" style="color:#36acaa">f1</span><span class="token plain"> </span><span class="token variable" style="color:#36acaa">unlocked</span><br></div></code></pre></div></div>
<p>If you have written code against Redis, you can write code against Kahuna in an afternoon. The complexity is real, but it is tested with Jepsen once on the server, instead of in every application in production.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="where-kahuna-does-not-belong">Where Kahuna Does Not Belong<a href="https://kahunakv.github.io/blog/redis-postgres-and-kahuna/#where-kahuna-does-not-belong" class="hash-link" aria-label="Direct link to Where Kahuna Does Not Belong" title="Direct link to Where Kahuna Does Not Belong" translate="no">​</a></h2>
<p>My friend was partly right. Kahuna is not a replacement for Postgres as your main database. Relational queries, reports, and bulk data belong there. It is not a replacement for Redis as a cache for data you can rebuild. Consensus is not free, and you should only pay for it where a wrong answer is worse than a slow answer.</p>
<p>The rule is simple. If any of these would be a real incident for you, the state belongs in Kahuna:</p>
<ul>
<li class="">Two workers owning the same job.</li>
<li class="">A confirmed write disappearing after a failover.</li>
<li class="">A retried request allocating a second ID.</li>
<li class="">A multi-key update becoming half visible.</li>
</ul>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="where-camusdb-fits">Where CamusDB Fits<a href="https://kahunakv.github.io/blog/redis-postgres-and-kahuna/#where-camusdb-fits" class="hash-link" aria-label="Direct link to Where CamusDB Fits" title="Direct link to Where CamusDB Fits" translate="no">​</a></h2>
<p>Kahuna did not start as a standalone product. It started as the piece that <a href="https://camusdb.github.io/" target="_blank" rel="noopener noreferrer" class="">CamusDB</a>, a distributed SQL database, needed underneath it: replication, consensus, multi-key transactions with real isolation, and a reliable place for metadata. I built that piece as Kahuna, and CamusDB became its first serious user.</p>
<p>That relationship is the main reason Kahuna keeps improving. Every CamusDB write is a Kahuna transaction. Every CamusDB replica is a Kahuna partition. When CamusDB needed ordered scans, Kahuna got key-range routing. When it needed stable reads while writes continued, Kahuna got snapshot holds. When it needed to recover unfinished transactions after a crash, Kahuna got durable commit decisions.</p>
<p>A coordination service that only stores feature flags is never pushed that hard. A SQL database on top of it pushes every day. That pressure turned Kahuna into something I was comfortable calling <a class="" href="https://kahunakv.github.io/blog/kahuna-1-0-stable/">1.0</a>. The <a href="https://camusdb.github.io/" target="_blank" rel="noopener noreferrer" class="">CamusDB documentation</a> and <a href="https://github.com/camusdb" target="_blank" rel="noopener noreferrer" class="">source code</a> are both public.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-answer-i-should-have-given">The Answer I Should Have Given<a href="https://kahunakv.github.io/blog/redis-postgres-and-kahuna/#the-answer-i-should-have-given" class="hash-link" aria-label="Direct link to The Answer I Should Have Given" title="Direct link to The Answer I Should Have Given" translate="no">​</a></h2>
<p>Could you do all of this with Redis and Postgres? Most of it, yes, if you write the glue code carefully, test it under failures, and keep it correct while different teams change it for years. I have done that. It works until the one night when it doesn't.</p>
<p>Kahuna is that glue code, written once, replicated, tested with Jepsen, and given a simple API. It is not a bigger Redis or a smaller Postgres. It is the layer in between, the one that every 24/7 system builds sooner or later.</p>
<p>I still think Redis and Postgres are great. I just don't want to write that glue code again.</p>
<p>Kahuna is <a href="https://github.com/kahunakv/kahuna" target="_blank" rel="noopener noreferrer" class="">open source</a> under the MIT license. If you have your own version of that glue code somewhere, I would love to hear how it broke.</p>]]></content:encoded>
            <category>kahuna</category>
            <category>camusdb</category>
            <category>redis</category>
            <category>postgres</category>
            <category>distributed-systems</category>
        </item>
        <item>
            <title><![CDATA[Kahuna 1.0: From Coordination Library to Distributed Database]]></title>
            <link>https://kahunakv.github.io/blog/kahuna-1-0-stable/</link>
            <guid>https://kahunakv.github.io/blog/kahuna-1-0-stable/</guid>
            <pubDate>Mon, 17 Aug 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[Kahuna started as a simple idea: build a lightweight tool that centralizes the distributed concerns every team eventually runs into. Locks, leader election, configuration, sequences. Things that are easy to get wrong and painful to debug when they break at 3 a.m.]]></description>
            <content:encoded><![CDATA[<p>Kahuna started as a simple idea: build a lightweight tool that centralizes the distributed concerns every team eventually runs into. Locks, leader election, configuration, sequences. Things that are easy to get wrong and painful to debug when they break at 3 a.m.</p>
<p>Somewhere along the way, it became something else entirely.</p>
<p>Today I am releasing Kahuna 1.0, and the system that ships is far more than the coordination library I set out to build. Kahuna is a distributed key/value database with transactions, a scripting engine, horizontal scalability, and enough correctness testing to bet real workloads on it.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-road-here">The Road Here<a href="https://kahunakv.github.io/blog/kahuna-1-0-stable/#the-road-here" class="hash-link" aria-label="Direct link to The Road Here" title="Direct link to The Road Here" translate="no">​</a></h2>
<p>When I wrote the <a class="" href="https://kahunakv.github.io/blog/welcome/">first post on this blog</a>, I described three pillars: a distributed key/value store, distributed locks, and a distributed sequencer. Those pillars are still here. But the foundation underneath them grew organically, each layer reinforcing the next.</p>
<p>The key/value store gained MVCC, snapshot isolation, serializable transactions, and a transaction coordinator that tracks reads, writes, and locks on the server instead of trusting the client. The lock system gained Jepsen-verified fencing tokens and lease semantics that survive leader changes. The sequencer gained retry-safe idempotency and range allocation.</p>
<p>Each of those features was built because a real problem demanded it, and each one made the next possible.</p>
<p>That is the thing about building infrastructure for distributed systems. You start solving one problem and discover that the solution is foundational for problems you had not considered yet.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="more-than-coordination">More Than Coordination<a href="https://kahunakv.github.io/blog/kahuna-1-0-stable/#more-than-coordination" class="hash-link" aria-label="Direct link to More Than Coordination" title="Direct link to More Than Coordination" translate="no">​</a></h2>
<p>The turning point was realizing that Kahuna's storage engine, consensus layer, and transaction machinery were general enough to support a distributed SQL database on top.</p>
<p><a href="https://github.com/camusdb" target="_blank" rel="noopener noreferrer" class="">CamusDB</a> is a distributed SQL database that uses Kahuna as its storage and coordination layer. It does not implement its own replication, its own consensus, or its own transaction protocol. It uses Kahuna's. Every CamusDB write is a Kahuna transaction. Every CamusDB replica is a Kahuna partition.</p>
<p>What started as a coordination tool, something closer to etcd or ZooKeeper with better ergonomics, had organically evolved into something with broader ambitions. The architecture that makes coordination correct, Raft consensus per partition, MVCC, distributed transactions with proper isolation, naturally extends into a solid foundation for a database.</p>
<p>A coordination system that can run a SQL database underneath it is a different kind of tool than one that only stores configuration keys. It means the transaction engine is real. It means the replication is not a toy. It means the consistency guarantees hold under workloads far heavier than lock renewals and feature flags.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="scalability-through-replication-factor">Scalability Through Replication Factor<a href="https://kahunakv.github.io/blog/kahuna-1-0-stable/#scalability-through-replication-factor" class="hash-link" aria-label="Direct link to Scalability Through Replication Factor" title="Direct link to Scalability Through Replication Factor" translate="no">​</a></h2>
<p>Full replication is simple: every node stores every partition. It works well for small clusters. It does not work when you want to add nodes to increase capacity instead of just increasing redundancy.</p>
<p>Kahuna 1.0 ships with configurable replication factor and a placement rebalancer. Set a replication factor of 3 on a 9-node cluster and each partition lives on 3 nodes instead of 9. Different partitions land on different subsets of the cluster. Adding nodes adds capacity. The rebalancer moves replicas gradually so catch-up traffic does not overwhelm foreground requests.</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token plain">kahuna-server </span><span class="token punctuation" style="color:#393A34">\</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  --initial-cluster https://kahuna-1:8082 https://kahuna-2:8082 https://kahuna-3:8082 </span><span class="token punctuation" style="color:#393A34">\</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  --raft-replication-factor </span><span class="token number" style="color:#36acaa">3</span><span class="token plain"> </span><span class="token punctuation" style="color:#393A34">\</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  --raft-enable-placement-rebalancer </span><span class="token boolean" style="color:#36acaa">true</span><br></div></code></pre></div></div>
<p>Zone-aware placement spreads replicas across racks or availability zones. Per-partition overrides let you increase the replication factor for critical data without changing the cluster default.</p>
<p>When your system manages hundreds of thousands of key/value entries and locks, full replication means every node carries the entire keyspace, every write fans out to every server. Replication factor lets Kahuna distribute that keyspace across many servers, so each node is responsible for a fraction of the data while the cluster as a whole absorbs the full workload. Adding servers increases both storage capacity and throughput.</p>
<p>This is the scaling model that makes Kahuna viable for production workloads beyond coordination. A system that forces every node to store everything hits a ceiling. Replication factor removes that ceiling.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="correctness-you-can-verify">Correctness You Can Verify<a href="https://kahunakv.github.io/blog/kahuna-1-0-stable/#correctness-you-can-verify" class="hash-link" aria-label="Direct link to Correctness You Can Verify" title="Direct link to Correctness You Can Verify" translate="no">​</a></h2>
<p>Distributed systems fail in boring ways. A leader that does not know it lost leadership serves stale reads. A fencing token goes backwards after a restart. Two transactions commit when no valid serial order can explain both.</p>
<p>Kahuna has a public <a href="https://github.com/kahunakv/kahuna-jepsen" target="_blank" rel="noopener noreferrer" class="">Jepsen test suite</a> that runs nightly against a five-node cluster. It exercises four workloads under network partitions, process kills, process pauses, and membership changes:</p>
<ul>
<li class="">Linearizable key/value registers</li>
<li class="">Lease-aware mutual exclusion with fencing token monotonicity</li>
<li class="">Serializable interactive transactions checked by Elle</li>
<li class="">Duplicate-free sequence allocation with idempotent replay</li>
</ul>
<p>The suite has already caught real bugs: stale reads from partitioned leaders, fencing tokens moving backwards after leader changes, lost updates in transactions, and write skew in modes where read validation should prevent it. Every one of those bugs is now fixed and covered by regression tests.</p>
<p>A green Jepsen run is evidence, not proof. But it is the kind of evidence that matters: adversarial testing under the failures that actually happen in production.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-ships-in-10">What Ships in 1.0<a href="https://kahunakv.github.io/blog/kahuna-1-0-stable/#what-ships-in-10" class="hash-link" aria-label="Direct link to What Ships in 1.0" title="Direct link to What Ships in 1.0" translate="no">​</a></h2>
<p>This release is the line where Kahuna moves from "interesting project" to "you can build on this":</p>
<ul>
<li class=""><strong>Distributed key/value store</strong> with persistent and ephemeral durability, revisions, compare-and-swap, expiration, and key-range routing</li>
<li class=""><strong>Distributed transactions</strong> with snapshot isolation or serializable consistency, optimistic or pessimistic locking, and a server-side transaction coordinator</li>
<li class=""><strong>Distributed locks</strong> with leases, fencing tokens, and replicated lock state</li>
<li class=""><strong>Distributed sequencer</strong> with retry-safe allocation, range reservation, and idempotency keys</li>
<li class=""><strong>Scripting engine</strong> for transactional logic that runs inside the cluster</li>
<li class=""><strong>Replication factor</strong> with zone-aware placement, per-partition overrides, and automatic rebalancing</li>
<li class=""><strong>Backups and point-in-time recovery</strong></li>
<li class=""><strong>Embedded node</strong> for integration tests and applications that need the transaction engine without the server</li>
<li class=""><strong>Jepsen-tested</strong> across key/value, lock, transaction, sequencer, and membership workloads</li>
<li class=""><strong>.NET and TypeScript clients</strong>, REST, gRPC, and CLI interfaces</li>
<li class=""><strong>MIT license</strong></li>
</ul>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-comes-next">What Comes Next<a href="https://kahunakv.github.io/blog/kahuna-1-0-stable/#what-comes-next" class="hash-link" aria-label="Direct link to What Comes Next" title="Direct link to What Comes Next" translate="no">​</a></h2>
<p>1.0 is stable, not finished.</p>
<p>The transaction coordinator will gain durable commit decisions for persistent workloads, so recovery after a crash can resolve in-flight transactions from the canonical record instead of asking the client to retry. Snapshot reads and snapshot holds need Jepsen coverage. Clock fault testing will move to isolated Linux hosts where time manipulation does not leak across containers.</p>
<p>There is also work to do on composed backups for clusters with per-partition placement, richer key-range operations, and continued performance tuning of the write coalescing and backend I/O scheduler.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="a-different-kind-of-tool">A Different Kind of Tool<a href="https://kahunakv.github.io/blog/kahuna-1-0-stable/#a-different-kind-of-tool" class="hash-link" aria-label="Direct link to A Different Kind of Tool" title="Direct link to A Different Kind of Tool" translate="no">​</a></h2>
<p>I started building Kahuna because my team needed a single place for locks, configuration, and sequences. Something simpler than running Redis for one thing, ZooKeeper for another, and a database table for a third.</p>
<p>What emerged is a distributed database engine that happens to be exceptionally good at coordination. The same Raft consensus, the same MVCC, and the same transaction protocol that make locks and sequences correct also make it possible to run a SQL database on top.</p>
<p>That organic evolution, from coordination library to database engine, turned out to be the best outcome.</p>
<p>Kahuna is <a href="https://github.com/kahunakv/kahuna" target="_blank" rel="noopener noreferrer" class="">open source</a> and ready for production use. If you are building distributed systems and tired of stitching together coordination from three different tools, give it a try.</p>]]></content:encoded>
            <category>kahuna</category>
            <category>release</category>
            <category>distributed-systems</category>
        </item>
        <item>
            <title><![CDATA[Breaking Kahuna on Purpose with Jepsen]]></title>
            <link>https://kahunakv.github.io/blog/jepsen-testing-kahuna/</link>
            <guid>https://kahunakv.github.io/blog/jepsen-testing-kahuna/</guid>
            <pubDate>Thu, 06 Aug 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[Distributed systems usually look reliable when everything is healthy. The network is fast, every server is online, and requests arrive in a convenient order.]]></description>
            <content:encoded><![CDATA[<p>Distributed systems usually look reliable when everything is healthy. The network is fast, every server is online, and requests arrive in a convenient order.</p>
<p>But what happens when a server loses contact with the rest of the cluster? What if a process stops while handling a request? What if Kahuna saves a value, but the reply never reaches the client?</p>
<p>We recently added a public <a href="https://github.com/kahunakv/kahuna-jepsen" target="_blank" rel="noopener noreferrer" class="">Jepsen test suite for Kahuna</a> to explore these difficult situations. It creates real Kahuna clusters, sends many requests at the same time, and introduces failures on purpose. It then checks whether Kahuna still keeps its promises.</p>
<p>Our goal is not only to see green test results. We want to find rare problems, understand them, and make sure they do not return.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-is-jepsen">What Is Jepsen?<a href="https://kahunakv.github.io/blog/jepsen-testing-kahuna/#what-is-jepsen" class="hash-link" aria-label="Direct link to What Is Jepsen?" title="Direct link to What Is Jepsen?" translate="no">​</a></h2>
<p><a href="https://github.com/jepsen-io/jepsen" target="_blank" rel="noopener noreferrer" class="">Jepsen</a> is a testing framework for distributed systems. It records everything that happens during a test: when each request starts, when it finishes, what result it returns, and which failures happen at the same time.</p>
<p>Jepsen also runs a special process called a nemesis. The name sounds dramatic, but its job is simple: create trouble. It can split the network, stop servers, pause processes, and later bring everything back.</p>
<p>At the end, Jepsen studies the complete history and asks an important question: can all these results be explained without breaking the system's guarantees?</p>
<p>This is more powerful than checking HTTP status codes. Imagine that a client sends a write, Kahuna commits it, and the network loses the reply. The client sees a timeout, but the write may still exist. Jepsen keeps this uncertainty in its analysis instead of guessing that the write failed.</p>
<p>Our tests run against a five-node Kahuna cluster. Client requests use a different network port from the internal <a class="" href="https://kahunakv.github.io/docs/architecture/raft/">Raft communication</a>. This allows us to interrupt replication between servers while clients can still send requests and observe the results.</p>
<p>The nightly tests use several kinds of failure:</p>
<ul>
<li class="">Network partitions separate some nodes from the others.</li>
<li class="">Process kills stop servers immediately.</li>
<li class="">Process pauses freeze a server without closing its connections.</li>
<li class=""><a class="" href="https://kahunakv.github.io/docs/server-configuration/#raft-membership-and-catch-up">Membership changes</a> remove a server from the cluster and add it again as a fresh member.</li>
</ul>
<p>These situations create the timing problems that are almost impossible to reproduce with normal tests.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="four-workloads-four-promises">Four Workloads, Four Promises<a href="https://kahunakv.github.io/blog/jepsen-testing-kahuna/#four-workloads-four-promises" class="hash-link" aria-label="Direct link to Four Workloads, Four Promises" title="Direct link to Four Workloads, Four Promises" translate="no">​</a></h2>
<p>There is no single test for all distributed-system behavior. A key/value store, a lock, a transaction, and a sequence generator each promise something different. Our suite therefore has four workloads.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="keyvalue-operations-behave-like-one-correct-copy">Key/value operations behave like one correct copy<a href="https://kahunakv.github.io/blog/jepsen-testing-kahuna/#keyvalue-operations-behave-like-one-correct-copy" class="hash-link" aria-label="Direct link to Key/value operations behave like one correct copy" title="Direct link to Key/value operations behave like one correct copy" translate="no">​</a></h3>
<p>The register workload sends reads, writes, and compare-and-set operations to different nodes in Kahuna's <a class="" href="https://kahunakv.github.io/docs/distributed-keyvalue-store/">distributed key/value store</a>.</p>
<p>Compare-and-set means “change the value only if it still equals the value I expect.” For example, two clients may both try to change a value from 4 to 5. Only an operation based on the current value should succeed.</p>
<p>Jepsen uses a checker called Knossos to test each key for <a href="https://jepsen.io/consistency/models/linearizable" target="_blank" rel="noopener noreferrer" class="">linearizability</a>. The word is technical, but the main idea is friendly: even though many servers and clients are involved, each successful operation should look as if it happened at one exact moment on one correct copy of the data.</p>
<p>Real-time order also matters. If a write finishes before a new read begins, the read must not return an older value.</p>
<p>This workload can catch an isolated server that still thinks it is the leader and returns stale data as if it were current.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="locks-keep-owners-apart-and-tokens-moving-forward">Locks keep owners apart and tokens moving forward<a href="https://kahunakv.github.io/blog/jepsen-testing-kahuna/#locks-keep-owners-apart-and-tokens-moving-forward" class="hash-link" aria-label="Direct link to Locks keep owners apart and tokens moving forward" title="Direct link to Locks keep owners apart and tokens moving forward" translate="no">​</a></h3>
<p><a class="" href="https://kahunakv.github.io/docs/distributed-locks/">Kahuna locks</a> use <a class="" href="https://kahunakv.github.io/docs/distributed-locks/leases/">leases</a>. A lease gives a client the lock for a limited time. If that client crashes, the lock can expire and another client can continue. Without expiration, one crashed client could block a resource forever.</p>
<p>Leases also create a challenge. An old client may wake up after its lease has expired and still believe it owns the lock. Kahuna uses <a class="" href="https://kahunakv.github.io/docs/distributed-locks/fencing-tokens/">fencing tokens</a> to protect other systems from this old owner. Every new owner receives a newer token, so a database or service can reject commands carrying an older one.</p>
<p>The lock workload checks two promises:</p>
<ul>
<li class="">Two different owners must not definitely hold the same valid lease at the same time.</li>
<li class="">Fencing tokens must never go backwards, and a new owner must receive a higher token.</li>
</ul>
<p>The checker understands that leases expire. It does not report a problem when an overlap can be explained by normal expiration or by a release whose reply was lost.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="transactions-behave-like-a-safe-serial-order">Transactions behave like a safe serial order<a href="https://kahunakv.github.io/blog/jepsen-testing-kahuna/#transactions-behave-like-a-safe-serial-order" class="hash-link" aria-label="Direct link to Transactions behave like a safe serial order" title="Direct link to Transactions behave like a safe serial order" translate="no">​</a></h3>
<p>The transaction workload runs <a class="" href="https://kahunakv.github.io/docs/distributed-keyvalue-store/transactions/">interactive transactions</a> that read lists and append unique values across several keys.</p>
<p>It uses a checker called Elle. Elle studies the relationships between transactions and looks for cycles that should be impossible. In simpler terms, it asks whether the completed transactions could have run one after another in some valid order, even though they actually ran at the same time.</p>
<p>This test can find problems that single-key checks cannot see:</p>
<ul>
<li class="">A lost update, where one successful write silently replaces another.</li>
<li class="">An aborted read, where data from a failed transaction becomes visible.</li>
<li class="">A dirty update, where a transaction builds on data that should never have committed.</li>
<li class="">Write skew, where two transactions make decisions from old information and together break the expected ordering.</li>
</ul>
<p>These bugs are difficult because each request may look correct by itself. The problem only becomes visible when we study the whole transaction history.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="sequences-never-give-the-same-value-to-two-clients">Sequences never give the same value to two clients<a href="https://kahunakv.github.io/blog/jepsen-testing-kahuna/#sequences-never-give-the-same-value-to-two-clients" class="hash-link" aria-label="Direct link to Sequences never give the same value to two clients" title="Direct link to Sequences never give the same value to two clients" translate="no">​</a></h3>
<p>Kahuna's <a class="" href="https://kahunakv.github.io/docs/distributed-sequencer/">distributed sequencer</a> can return one number or reserve a range of numbers. Clients can also retry a request with the same <a class="" href="https://kahunakv.github.io/docs/distributed-sequencer/idempotency/">idempotency key</a>. An idempotency key tells the server, “this is the same logical request again, not a new one.”</p>
<p>The sequencer workload checks that:</p>
<ul>
<li class="">Two successful allocations never contain the same value.</li>
<li class="">A reserved range has the size requested by the client.</li>
<li class="">Retrying with the same idempotency key returns the original allocation.</li>
</ul>
<p>The test allows gaps and values that arrive out of order. This is intentional. Kahuna gives nodes blocks of values that they can allocate quickly. A leader change may leave part of a block unused, creating a gap. That is safe because the important promise is uniqueness, not a perfect sequence with no missing numbers.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-does-a-green-result-mean">What Does a Green Result Mean?<a href="https://kahunakv.github.io/blog/jepsen-testing-kahuna/#what-does-a-green-result-mean" class="hash-link" aria-label="Direct link to What Does a Green Result Mean?" title="Direct link to What Does a Green Result Mean?" translate="no">​</a></h2>
<p>A green Jepsen result is strong evidence, but it is not a mathematical proof that every possible execution is correct.</p>
<p>It means that, for the requests, load, configuration, duration, and failures in that run, the checker found no broken guarantee. Another timing pattern may still reveal a rare bug. This is why we repeat tests with different request rates and shorter or longer periods between failures.</p>
<p>We also need to test the testers. During this work, our lock checker reported mutual-exclusion problems that did not really happen. It extended a client's lock period after a release reply was lost. We corrected the logic and added small test histories with known good and bad results. These negative controls prove that the checker can still detect a real violation.</p>
<p>An empty test is not a successful test either. If no transaction commits, there is nothing useful to analyze. If the sequencer returns too few values, its result is unknown. If the linearizability checker runs out of memory before it finishes, that result is also unknown—not green.</p>
<p>Being honest about these limits is an important part of reliable testing.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-tests-have-already-helped">The Tests Have Already Helped<a href="https://kahunakv.github.io/blog/jepsen-testing-kahuna/#the-tests-have-already-helped" class="hash-link" aria-label="Direct link to The Tests Have Already Helped" title="Direct link to The Tests Have Already Helped" translate="no">​</a></h2>
<p>The new suite started finding useful problems almost immediately.</p>
<p>The key/value workload found stale reads from a node that was separated from the majority. The node refused writes because it could not replicate them, which was correct. However, it still returned old values for reads as normal successful responses. Kahuna now confirms its leadership with the cluster before serving this kind of local read.</p>
<p>The lock workload found fencing tokens moving backwards after leader changes. One issue involved committed data during a restart. Another involved a new leader reading state before pending storage work was fully available. These failures needed a specific combination of timing and leadership changes, which made them difficult to find with ordinary tests.</p>
<p>The transaction workload found lost updates and data from aborted transactions becoming visible. It also found write skew in a mode where read validation should stop it. These investigations improved how Kahuna validates read-modify-write operations and how it records the final result when commit and rollback compete.</p>
<p>Jepsen also found less obvious problems. Some temporary network failures escaped as HTTP 500 responses instead of asking the client to retry. Some runs looked clean only because no useful operation finished. We even found a bug in our own lock checker.</p>
<p>Finding a checker bug is not wasted time. The test suite is software too, and we must verify its assumptions with the same care that we apply to Kahuna.</p>
<p>Every run keeps its history, timeline, latency charts, result details, and server logs. This gives us evidence that connects a visible failure to a particular event in the cluster. It is much more useful than a message that only says “the test failed.”</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-it-runs-nightly">Why It Runs Nightly<a href="https://kahunakv.github.io/blog/jepsen-testing-kahuna/#why-it-runs-nightly" class="hash-link" aria-label="Direct link to Why It Runs Nightly" title="Direct link to Why It Runs Nightly" translate="no">​</a></h2>
<p>The Jepsen suite runs every night and can also be started manually against a selected version of Kahuna. Different jobs combine the four workloads with different failures. Membership testing runs for longer because removing and safely adding a cluster member can take one or two minutes.</p>
<p>We do not plan to make Jepsen a required check for every pull request. These tests are expensive and intentionally unpredictable. A slow shared runner can create extra timeouts and a history that is too large to analyze. A noisy required check soon becomes a check that everybody ignores.</p>
<p>A failed nightly run asks us to download the artifacts and investigate. A clean run adds more confidence. Neither one replaces unit tests, focused regression tests, code review, or <a class="" href="https://kahunakv.github.io/docs/benchmarking/">performance benchmarks</a>.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-comes-next">What Comes Next?<a href="https://kahunakv.github.io/blog/jepsen-testing-kahuna/#what-comes-next" class="hash-link" aria-label="Direct link to What Comes Next?" title="Direct link to What Comes Next?" translate="no">​</a></h2>
<p>The next major workload will focus on <a class="" href="https://kahunakv.github.io/docs/distributed-keyvalue-store/snapshot-holds/">snapshot reads and snapshot holds</a>. A snapshot lets a transaction read data as it existed at an earlier logical time, while new writes continue in the cluster. We want to verify that the old view stays stable and that snapshot holds protect the required <a class="" href="https://kahunakv.github.io/docs/internals/mvcc/">MVCC history</a> correctly.</p>
<p>Clock faults are another important area. Kahuna uses <a class="" href="https://kahunakv.github.io/docs/architecture/hybrid-logical-clocks/">hybrid logical clocks</a> for snapshots and lock leases. Moving time inside Docker Desktop can affect the whole virtual machine, so we will run these experiments on disposable Linux hosts where the clock changes are isolated.</p>
<p>We have also added a recovery checker. It measures the time between the end of a failure and the first successful request, adding another view of Kahuna's <a class="" href="https://kahunakv.github.io/docs/internals/replication/">replication and recovery behavior</a>. This helps us separate two questions:</p>
<ul>
<li class="">Is the cluster returning incorrect results?</li>
<li class="">Is the cluster correct but taking too long to become available again?</li>
</ul>
<p>We need more recovery data from runs where transactions make no progress. This will show whether the failure schedule is faster than normal recovery or whether the cluster sometimes gets stuck while finding a leader.</p>
<p>Other follow-up work includes handling one remaining retryable HTTP/2 error correctly, gathering more evidence for the new sequencer workload, testing membership changes with more workloads, and trying different levels of load and fault timing.</p>
<p>When Jepsen discovers a bug, we also want to create a smaller and more predictable regression test whenever possible. Jepsen is excellent at finding a surprising order of events. A focused test is better for checking that the same bug never returns.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="better-confidence-starts-with-better-questions">Better Confidence Starts with Better Questions<a href="https://kahunakv.github.io/blog/jepsen-testing-kahuna/#better-confidence-starts-with-better-questions" class="hash-link" aria-label="Direct link to Better Confidence Starts with Better Questions" title="Direct link to Better Confidence Starts with Better Questions" translate="no">​</a></h2>
<p>Jepsen does not make Kahuna correct on its own. It helps us ask better questions:</p>
<ul>
<li class="">Can an isolated leader return old data as current?</li>
<li class="">Can a fencing token move backwards after a restart?</li>
<li class="">Can two transactions commit when no safe order can explain both?</li>
<li class="">Can a sequence value be returned twice after a lost reply?</li>
<li class="">How long does the cluster need to recover after a failure?</li>
</ul>
<p>These questions tell us much more than “did the servers stay online?”</p>
<p>Kahuna is a coordination system, so its behavior during uncertainty is essential. By breaking the cluster on purpose and carefully checking what happened, we can turn difficult failure scenarios into useful evidence—and use that evidence to keep improving Kahuna.</p>]]></content:encoded>
            <category>kahuna</category>
            <category>jepsen</category>
            <category>testing</category>
            <category>distributed-systems</category>
        </item>
        <item>
            <title><![CDATA[Making Distributed Transactions Easier to Trust]]></title>
            <link>https://kahunakv.github.io/blog/reusable-transaction-coordinator/</link>
            <guid>https://kahunakv.github.io/blog/reusable-transaction-coordinator/</guid>
            <pubDate>Sat, 11 Jul 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[Distributed transactions rarely fail in dramatic ways. They fail in boring ways.]]></description>
            <content:encoded><![CDATA[<p>Distributed transactions rarely fail in dramatic ways. They fail in boring ways.</p>
<p>A client writes key <code>account:alice</code>, the response gets lost, the client retries, another request starts commit, and now the system has to answer a simple question: did that write count?</p>
<p>That is the kind of question Kahuna should answer on the server, not push onto every client library and application.</p>
<p>The next step for Kahuna transactions is a reusable transaction coordinator: one server-owned place that tracks what a transaction has read, written, locked, retried, committed, or rolled back.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-problem-with-just-send-the-list">The Problem With "Just Send the List"<a href="https://kahunakv.github.io/blog/reusable-transaction-coordinator/#the-problem-with-just-send-the-list" class="hash-link" aria-label="Direct link to The Problem With &quot;Just Send the List&quot;" title="Direct link to The Problem With &quot;Just Send the List&quot;" translate="no">​</a></h2>
<p>Interactive transactions are tempting to implement like this:</p>
<ol>
<li class="">The client reads and writes keys.</li>
<li class="">The client remembers every key it touched.</li>
<li class="">At commit time, the client sends the final list to the server.</li>
</ol>
<p>That works until it doesn’t.</p>
<p>If the client forgets one modified key, commit can look valid while leaving part of the transaction outside the decision. If the client misses a lock, cleanup can leak. If the client loses a response, it may not know whether an operation happened.</p>
<p>That is too much trust in the least reliable part of the system: the caller sitting on the other side of a network.</p>
<p>The coordinator changes the rule. The client keeps an opaque transaction handle. The server keeps the transaction truth.</p>
<p>When a write succeeds, the coordinator records it. When a lock is acquired, the coordinator records it. When a read needs validation, the coordinator records the dependency. Commit uses the server-owned working set, not a last-minute summary from the client.</p>
<p>For developers, the API gets smaller. For Kahuna, the protocol gets harder to misuse.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="commit-starts-by-closing-the-door">Commit Starts by Closing the Door<a href="https://kahunakv.github.io/blog/reusable-transaction-coordinator/#commit-starts-by-closing-the-door" class="hash-link" aria-label="Direct link to Commit Starts by Closing the Door" title="Direct link to Commit Starts by Closing the Door" translate="no">​</a></h2>
<p>The nastiest transaction bug is the one that arrives half a millisecond late.</p>
<p>Imagine this:</p>
<ol>
<li class="">Request A starts writing <code>cart:123</code>.</li>
<li class="">Request B starts committing the transaction.</li>
<li class="">Request A finishes after commit already captured the working set.</li>
</ol>
<p>If <code>cart:123</code> is missing from commit, the transaction is wrong. If commit keeps waiting for more requests forever, the transaction is stuck.</p>
<p>The coordinator will make finalization explicit. The first close, commit, or rollback request closes the transaction to new operations. Operations that were already registered are allowed to finish. Then the coordinator freezes the working set.</p>
<p>That frozen view is the one every later commit or rollback attempt must use.</p>
<p>The useful mental model is simple: close the door, wait for everyone already inside, then make the decision.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="safe-retries-for-every-operation">Safe Retries for Every Operation<a href="https://kahunakv.github.io/blog/reusable-transaction-coordinator/#safe-retries-for-every-operation" class="hash-link" aria-label="Direct link to Safe Retries for Every Operation" title="Direct link to Safe Retries for Every Operation" translate="no">​</a></h2>
<p>Distributed systems force clients to ask uncomfortable questions:</p>
<ul>
<li class="">Did the server never receive my write?</li>
<li class="">Did it receive it and fail?</li>
<li class="">Did it apply it, then lose the response?</li>
</ul>
<p>Those cases look identical from the client when the connection drops.</p>
<p>Each transaction operation will therefore have a stable operation ID. One logical request gets one ID. If the client retries, it reuses the same ID.</p>
<p>That lets Kahuna recognize a retry instead of treating it as a fresh command. If the first response was lost, the participant can return the previous result without applying the write again. If the same ID is reused for a different command, Kahuna rejects it.</p>
<p>There is an important trade-off here. If the coordinator knows an operation may still be running but cannot confirm the outcome, commit should wait or ask the caller to retry. A transaction that pauses is annoying. A transaction that silently drops a write is much worse.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="a-reusable-core-not-a-second-script-engine">A Reusable Core, Not a Second Script Engine<a href="https://kahunakv.github.io/blog/reusable-transaction-coordinator/#a-reusable-core-not-a-second-script-engine" class="hash-link" aria-label="Direct link to A Reusable Core, Not a Second Script Engine" title="Direct link to A Reusable Core, Not a Second Script Engine" translate="no">​</a></h2>
<p>Kahuna already has serious transaction machinery inside its scripting path: read validation, two-phase commit, lock cleanup, rollback behavior, and conflict handling.</p>
<p>The problem is that some of this logic is too close to the script executor.</p>
<p>Scripts should handle scripts: parsing, variables, expressions, and script-specific execution.</p>
<p>The reusable coordinator should handle transactions: identity, lifecycle, working sets, operation registration, finalization, retries, and cleanup.</p>
<p>Once those responsibilities are separated, the script engine becomes one user of the coordinator. The .NET and TypeScript clients become other users of the same protocol without copying script-specific behavior.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="finalization-is-a-state-machine">Finalization Is a State Machine<a href="https://kahunakv.github.io/blog/reusable-transaction-coordinator/#finalization-is-a-state-machine" class="hash-link" aria-label="Direct link to Finalization Is a State Machine" title="Direct link to Finalization Is a State Machine" translate="no">​</a></h2>
<p>Commit and rollback are not buttons. They are state transitions under pressure.</p>
<p>The coordinator has to handle duplicate commits, commit racing rollback, retries arriving late, and automatic cleanup for expired sessions.</p>
<p>The rule should be boring and strict:</p>
<ul>
<li class="">If finalization is already running, duplicate requests wait for the same result.</li>
<li class="">If the transaction commits, it stays committed.</li>
<li class="">If the transaction rolls back, it stays rolled back.</li>
<li class="">If the failure is retryable, the finalization slot opens again for a later attempt.</li>
</ul>
<p>That last point matters. A temporary routing error should not permanently consume the one chance to commit.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="concurrency-choices-should-be-explicit">Concurrency Choices Should Be Explicit<a href="https://kahunakv.github.io/blog/reusable-transaction-coordinator/#concurrency-choices-should-be-explicit" class="hash-link" aria-label="Direct link to Concurrency Choices Should Be Explicit" title="Direct link to Concurrency Choices Should Be Explicit" translate="no">​</a></h2>
<p>Not every application wants the same transaction behavior.</p>
<p>Some workloads want pessimistic locking because conflicts are expensive. Others prefer optimistic concurrency because most transactions do not collide. Some systems already have a snapshot timestamp and do not need Kahuna to repeat the same read validation.</p>
<p>So a transaction should begin with an explicit policy:</p>
<ul>
<li class="">How reads are validated</li>
<li class="">Whether locks are pessimistic or optimistic</li>
<li class="">How long the transaction may live</li>
<li class="">When locks are released</li>
<li class="">Whether the commit decision must be durable</li>
</ul>
<p>Invalid combinations should fail early. Guessing is not a transaction feature.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="durable-commit-decisions-with-honest-limits">Durable Commit Decisions, with Honest Limits<a href="https://kahunakv.github.io/blog/reusable-transaction-coordinator/#durable-commit-decisions-with-honest-limits" class="hash-link" aria-label="Direct link to Durable Commit Decisions, with Honest Limits" title="Direct link to Durable Commit Decisions, with Honest Limits" translate="no">​</a></h2>
<p>The hardest moment is not commit. It is commit after the decision was made, before every participant heard it.</p>
<p>For persistent data, Kahuna can offer an opt-in durable decision mode.</p>
<p>Before resolving participant writes, the coordinator initializes a canonical transaction record, replicates prepared intents for each persistent key, validates the read set, and then decides the record as <code>Commit</code> or <code>Abort</code>. Recovery can later resolve unfinished intents from that record after a restart or leadership change.</p>
<p>The order is the guarantee:</p>
<ol>
<li class="">The canonical transaction record is initialized as <code>Undecided</code>.</li>
<li class="">Prepared intents are replicated on participant partitions.</li>
<li class="">The canonical record is decided as <code>Commit</code> or <code>Abort</code>.</li>
<li class="">Participants materialize committed values with completion receipts, or discard aborted intents.</li>
</ol>
<p>That prevents an old retry from turning “already committed” into “unknown.”</p>
<p>This mode costs more coordination, so it should not be the default for every workload. It also cannot cover ephemeral values. If data only lives in memory, a durable commit decision cannot make that memory survive a process loss.</p>
<p>It also does not make every intermediate state durable. The interactive session before durable finalization still lives in memory. Once prepared persistent intents exist, participant leader changes do not lose staged values; recovery can resolve them from the canonical record. If the coordinator disappears before the record exists, the honest answer may still be “retry,” not “we know exactly what happened.”</p>
<p>That honesty is part of the design.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-this-means-for-clients">What This Means for Clients<a href="https://kahunakv.github.io/blog/reusable-transaction-coordinator/#what-this-means-for-clients" class="hash-link" aria-label="Direct link to What This Means for Clients" title="Direct link to What This Means for Clients" translate="no">​</a></h2>
<p>Client-side transaction code should become lighter.</p>
<p>The client still keeps a handle, a policy, operation IDs, and local status. It no longer needs to be the authority for keys, locks, or read dependencies.</p>
<p>Other systems built on Kahuna can remove duplicate coordination state too. They may still own their schemas, isolation rules, cache protocols, and application timeouts. Kahuna should own the mechanics that are easy to get wrong:</p>
<ul>
<li class="">Operation fencing</li>
<li class="">Working-set tracking</li>
<li class="">Commit and rollback serialization</li>
<li class="">Retry handling</li>
<li class="">Lock cleanup</li>
<li class="">Expiration cleanup</li>
</ul>
<p>There is a cost. Registering and confirming operations adds coordinator traffic. The first target is correctness. Then we can measure point operations, batches, validated reads, and snapshot reads to decide where batching or locality should reduce overhead.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-payoff">The Payoff<a href="https://kahunakv.github.io/blog/reusable-transaction-coordinator/#the-payoff" class="hash-link" aria-label="Direct link to The Payoff" title="Direct link to The Payoff" translate="no">​</a></h2>
<p>The goal is not to make failures disappear. That is not how distributed systems work.</p>
<p>The goal is to make every failure land in one of a few understandable boxes:</p>
<ul>
<li class="">Already applied, here is the same result</li>
<li class="">Still running, retry the same operation</li>
<li class="">Closed, no new operations accepted</li>
<li class="">Committed, and that decision will not change</li>
<li class="">Rolled back, and that decision will not change</li>
<li class="">Unknown within this mode, retry at the application boundary</li>
</ul>
<p>That is the kind of simplicity Kahuna should provide: fewer ways for users to make a distributed transaction unsafe, and one reusable coordinator where the hard rules can be tested under concurrency, retries, leadership changes, and process failures.</p>]]></content:encoded>
            <category>kahuna</category>
            <category>transactions</category>
            <category>distributed-systems</category>
        </item>
        <item>
            <title><![CDATA[Why I Built Kahuna: A New Take on Distributed Key/Value Storage]]></title>
            <link>https://kahunakv.github.io/blog/welcome/</link>
            <guid>https://kahunakv.github.io/blog/welcome/</guid>
            <pubDate>Tue, 25 Mar 2025 00:00:00 GMT</pubDate>
            <description><![CDATA[About eight years ago, I had the opportunity to build systems for my company using Erlang. Discovering the actor model was a turning point in how I designed systems from that moment on. However, for strategic reasons, we had to move away from Erlang and focus on the .NET ecosystem. While robust and well-established actor model libraries for C# exist, they included many features we didn’t need, and their performance wasn’t entirely satisfactory for our use cases.]]></description>
            <content:encoded><![CDATA[<p>About eight years ago, I had the opportunity to build systems for my company using <a href="https://www.erlang.org/" target="_blank" rel="noopener noreferrer" class="">Erlang</a>. Discovering the actor model was a turning point in how I designed systems from that moment on. However, for strategic reasons, we had to move away from Erlang and focus on the .NET ecosystem. While robust and well-established actor model libraries for C# exist, they included many features we didn’t need, and their performance wasn’t entirely satisfactory for our use cases.
This led me to the idea of building a lightweight library.</p>
<p>The goal of creating a simple library led me to develop <a href="https://github.com/andresgutierrez/nixie" target="_blank" rel="noopener noreferrer" class="">Nixie</a>. By leveraging <a href="https://learn.microsoft.com/en-us/dotnet/standard/collections/thread-safe/" target="_blank" rel="noopener noreferrer" class="">lock-free structures</a>, strongly typed actors, low-level programming, and the <a href="https://learn.microsoft.com/en-us/dotnet/standard/parallel-programming/task-parallel-library-tpl" target="_blank" rel="noopener noreferrer" class="">Task Processing Library (TPL)</a>, I built a powerful foundation for application development. It immediately delivered the results we expected: fewer errors and higher performance.</p>
<p>Later, the need arose to develop a leader election library for one of our critical systems, one that wouldn’t rely on an external system, would offer fault tolerance (electing a new leader if the current one failed or went down), and could be integrated into our .NET projects. It was also essential to support multiple partitions (sometimes called regions, groups, or tablets) to effectively utilize computing power without sacrificing consistency. The learning experience and hands-on work with Raft led me to open-source Kommander.</p>
<p><a href="https://github.com/andresgutierrez/kommander" target="_blank" rel="noopener noreferrer" class="">Kommander</a> is a distributed consensus library for C#/.NET based on <a href="https://raft.github.io/" target="_blank" rel="noopener noreferrer" class="">Raft</a>. It supports multiple Raft groups, each with its own leader, followers, and replication log. It enables node communication via <a href="https://grpc.io/" target="_blank" rel="noopener noreferrer" class="">gRPC</a> by default, with REST/JSON also available for environments that need HTTP protocol flexibility or easier debugging. For the persistent write-ahead log, it uses <a href="https://rocksdb.org/" target="_blank" rel="noopener noreferrer" class="">RocksDB</a> as an embedded database, with SQLite also available as an option.</p>
<p>Kommander handles processes like leader election, replication, compaction, and membership changes using lightweight state machines and asynchronous communication. This approach enables hundreds or even thousands of partitions, leveraging multi-core servers efficiently. It also helps avoid multithreading concurrency issues, reducing the risk of hard-to-reproduce errors and lock contention.</p>
<p>With Kommander providing a solid foundation, I recognized additional needs that could be addressed with a new tool based on this technology. This led to the idea of building an auxiliary tool to centralize distributed concerns such as locks, leader election, configuration management, sessions, caching, sequencing, and more.</p>
<p>In the past, we relied on various tools like Redis, Zookeeper, and even database tables to solve these challenges. However, it was easy to misuse a tool or assume it provided guarantees or performance levels that it actually didn’t.</p>
<p>Taking all of this into account, I created Kahuna.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-the-name">Why the Name?<a href="https://kahunakv.github.io/blog/welcome/#why-the-name" class="hash-link" aria-label="Direct link to Why the Name?" title="Direct link to Why the Name?" translate="no">​</a></h2>
<p>The name "Kahuna" comes from a Hawaiian word traditionally used to describe an expert in a field, a fitting metaphor for a tool that aims to be the expert all services consult for coordination and operation. Inspired by battle-tested distributed systems, Kahuna is more than just another key/value store.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-net-breaking-the-language-paradigm">Why .NET? Breaking the Language Paradigm<a href="https://kahunakv.github.io/blog/welcome/#why-net-breaking-the-language-paradigm" class="hash-link" aria-label="Direct link to Why .NET? Breaking the Language Paradigm" title="Direct link to Why .NET? Breaking the Language Paradigm" translate="no">​</a></h2>
<p>One of my primary motivations for building Kahuna was to demonstrate that powerful, low-level systems infrastructure isn’t limited to C++, Rust, Java, or Go. By implementing Kahuna in modern C# and .NET technologies, I wanted to challenge the prevailing notion that high-performance distributed systems can only be built with traditional systems programming languages. This isn’t a hard rule. There are great tools like Garnet, recently led by Microsoft. Similarly, in Java, a language with a garbage collector, you can find powerful distributed systems like Kafka, Cassandra, and Spark. This choice reflects not just a technical preference but a broader vision of expanding the distributed systems development ecosystem.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-three-pillars-of-kahuna">The Three Pillars of Kahuna<a href="https://kahunakv.github.io/blog/welcome/#the-three-pillars-of-kahuna" class="hash-link" aria-label="Direct link to The Three Pillars of Kahuna" title="Direct link to The Three Pillars of Kahuna" translate="no">​</a></h2>
<p>Kahuna brings together three critical distributed system primitives in one platform:</p>
<ol>
<li class="">A distributed key/value store for metadata storage, configuration, caching, sessions, and more.</li>
<li class="">A distributed locking system for coordinating access to shared resources in a cluster.</li>
<li class="">A distributed sequencer for generating globally ordered events, crucial for ensuring consistency in distributed workflows.</li>
</ol>
<p>Many systems provide these functionalities separately, but I wanted a single, unified solution that integrates all three while maintaining strong consistency, high availability, simplicity, and efficient scalability.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-kahuna">Why Kahuna?<a href="https://kahunakv.github.io/blog/welcome/#why-kahuna" class="hash-link" aria-label="Direct link to Why Kahuna?" title="Direct link to Why Kahuna?" translate="no">​</a></h2>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="distributed-transactions-with-snapshot-isolation-or-serializable-consistency">Distributed Transactions with Snapshot Isolation or Serializable Consistency<a href="https://kahunakv.github.io/blog/welcome/#distributed-transactions-with-snapshot-isolation-or-serializable-consistency" class="hash-link" aria-label="Direct link to Distributed Transactions with Snapshot Isolation or Serializable Consistency" title="Direct link to Distributed Transactions with Snapshot Isolation or Serializable Consistency" translate="no">​</a></h3>
<p>Kahuna supports multi-node distributed transactions with optimistic or pessimistic locking, allowing developers to balance performance and strict consistency. Transactions can achieve either:</p>
<ul>
<li class="">Snapshot isolation, MVCC-based, for high-performance reads without conflicts.</li>
<li class="">Serializable consistency for the strongest level of isolation.</li>
</ul>
<p>This makes Kahuna ideal for applications requiring strict data integrity guarantees in highly concurrent environments.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="two-durability-modes-persistent--ephemeral">Two Durability Modes: Persistent &amp; Ephemeral<a href="https://kahunakv.github.io/blog/welcome/#two-durability-modes-persistent--ephemeral" class="hash-link" aria-label="Direct link to Two Durability Modes: Persistent &amp; Ephemeral" title="Direct link to Two Durability Modes: Persistent &amp; Ephemeral" translate="no">​</a></h3>
<p>Not all data needs permanent storage. Kahuna supports two durability modes to optimize storage and performance:</p>
<ul>
<li class="">Persistent mode: Data is replicated across nodes for durability and fault tolerance, ensuring high availability even during failures.</li>
<li class="">Ephemeral mode: Designed for short-lived objects such as caching, short sessions, and temporary leases/locks, ensuring lightweight storage without unnecessary replication overhead.</li>
</ul>
<p>This flexibility allows developers to use Kahuna for both long-term state storage and temporary coordination needs.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="a-more-powerful-approach-to-distributed-locking">A More Powerful Approach to Distributed Locking<a href="https://kahunakv.github.io/blog/welcome/#a-more-powerful-approach-to-distributed-locking" class="hash-link" aria-label="Direct link to A More Powerful Approach to Distributed Locking" title="Direct link to A More Powerful Approach to Distributed Locking" translate="no">​</a></h3>
<p>Distributed locking is notoriously difficult to get right, and perfect distributed locks are nearly impossible. Kahuna provides tools to help developers mitigate edge cases, including:</p>
<ul>
<li class="">Persisted lock state: Ensuring another node can maintain the lock state if the leader fails.</li>
<li class="">Leases with expiration: Preventing locks from persisting indefinitely if the holder crashes.</li>
<li class="">Fencing tokens: Preventing stale lock holders from causing race conditions.</li>
</ul>
<p>These mechanisms greatly reduce the risk of deadlocks, split-brain scenarios, and other failure conditions common in distributed environments.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="scripting-language-integration">Scripting Language Integration<a href="https://kahunakv.github.io/blog/welcome/#scripting-language-integration" class="hash-link" aria-label="Direct link to Scripting Language Integration" title="Direct link to Scripting Language Integration" translate="no">​</a></h2>
<p>A unique feature of Kahuna is its built-in scripting language, allowing developers to create consistent, transactional code that interacts directly with the key/value store.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="strong-consistency-with-raft-based-consensus">Strong Consistency with Raft-Based Consensus<a href="https://kahunakv.github.io/blog/welcome/#strong-consistency-with-raft-based-consensus" class="hash-link" aria-label="Direct link to Strong Consistency with Raft-Based Consensus" title="Direct link to Strong Consistency with Raft-Based Consensus" translate="no">​</a></h3>
<p>Kahuna ensures strong consistency using the Raft consensus algorithm, with each partition managed by its own Raft group. This design provides:</p>
<ul>
<li class="">Fault tolerance: Automatic leader election and replication ensure high availability.</li>
<li class="">Consistency guarantees: Every write is strongly consistent across multiple replicas.</li>
</ul>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="scalability-through-dynamic-partitioning">Scalability Through Dynamic Partitioning<a href="https://kahunakv.github.io/blog/welcome/#scalability-through-dynamic-partitioning" class="hash-link" aria-label="Direct link to Scalability Through Dynamic Partitioning" title="Direct link to Scalability Through Dynamic Partitioning" translate="no">​</a></h3>
<p>Kahuna is horizontally scalable, meaning it expands with infrastructure growth. It supports:</p>
<ul>
<li class="">Dynamic partitioning, similar to sharding, allowing for load balancing across nodes.</li>
<li class="">Automatic rebalancing, ensuring even workload distribution.</li>
</ul>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="kahuna-a-unified-solution-for-modern-distributed-applications">Kahuna: A Unified Solution for Modern Distributed Applications<a href="https://kahunakv.github.io/blog/welcome/#kahuna-a-unified-solution-for-modern-distributed-applications" class="hash-link" aria-label="Direct link to Kahuna: A Unified Solution for Modern Distributed Applications" title="Direct link to Kahuna: A Unified Solution for Modern Distributed Applications" translate="no">​</a></h2>
<p>Kahuna provides a powerful yet flexible foundation for building reliable, scalable, and highly available distributed applications. By integrating storage, coordination, and sequencing into one system, it reduces complexity and offers developers a consistent, battle-tested approach to managing distributed workloads.</p>
<p>Kahuna is now open-source, and I’d love to hear your thoughts. Whether you need a transactional key/value store, a robust distributed lock system, or a global event sequencer, Kahuna is built to handle it all.</p>]]></content:encoded>
            <category>kahuna</category>
        </item>
    </channel>
</rss>